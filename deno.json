{
  "tasks": {
    "test": "deno test --allow-net --allow-env",
    "test:cov": "deno test -A --coverage=coverage && deno coverage ./coverage --lcov --output=coverage.lcov",
    "cache": "deno cache ./deps.ts"
  },
  "fmt": {
    "files": {
      "exclude": ["*.md", "LICENSE", "*.json"]
    }
  },
  "lint": {
    "files": {
      "exclude": ["*.md", "LICENSE", "*.json"]
    }
  }
}
